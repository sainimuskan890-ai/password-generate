# password-generate
This Python code generates a random password by combining letters, numbers, and special characters, then selecting characters randomly to create a secure password of a specified length
